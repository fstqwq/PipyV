#include "io.h"
int fib(int x) {
	if (x <= 1) return x;
	return fib(x - 1) + fib(x - 2);
}
int main() {outl(fib(11));}
/*#include "io.h"
int f(int);
int fib(int);
int f(int x) {
	if (x <= 1) return x;
	outl(x); print("\n");
	return fib(x - 1) + fib(x - 2);
}
int fib(int x) {
	if (x <= 1) return x;
	outl(x); print("\n");
	return f(x - 1) + f(x - 2);
}

int main() {
	outl(fib(3));
}*/
/*

#include "io.h"
int f[2801];
int main() {
	print("start\n");
	int a = 10000;
	int b = 0;
	int c = 2800;
	int d = 0;
	int e = 0;
	int g = 0;

	for (;b-c!=0;) 
		f[b++] = a/5;
	print("stage 1\n");
	for (;; e = d%a){
		d = 0;
		g = c*2;
		if (g==0) break;
		
		for (b=c;;d=d*b){
			d=d+f[b]*a;
			f[b] = d%--g;
			d=d/g--;
			if (--b==0) break;
		}
		
		c = c-14;
		outl(e+d/a);
	}
	
  print("\n");
  return 0;
}*/
